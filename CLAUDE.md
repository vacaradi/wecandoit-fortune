# 위캔두잇 오늘의 운세와 명언, 그리고 부적

## 이 프로젝트가 뭔가

마법사 토끼 **'위'** 가 수정구로 오늘을 봐주는 운세 서비스.
PC와 스마트폰에서 같은 화면으로 돌아가는 반응형 웹앱.

```
생년월일 입력 → 운세 → 명언 → 부적 → 오늘의 넷플릭스
```

## 가장 중요한 원칙

```
비주얼은 진지하고 고급스럽다  ×  텍스트는 웃기다
```

이 낙차가 서비스의 전부다. **비주얼까지 웃기면 그냥 싼 앱이 된다.**
진지한 명조체로 "냉장고가 닫히게 하소서"라고 적혀 있는 것 — 이게 정체성.

## 캐릭터

| 이름 | 모습 | 등장 |
|---|---|---|
| **위** | 마법사 토끼. 남색 별무늬 로브, 수정구 | 인트로·운세·부적·넷플릭스 화면 |
| (미정) | 흰 도포 도사. 두루마리 | 명언 화면 |

캐릭터 이름을 함부로 지어내지 말 것. 확인되지 않았으면 물어본다.

## 디자인 토큰

영상에서 추출한 값. 임의로 바꾸지 않는다.

```
--ink       #04060C   우주 배경 (영상 배경색과 동일해서 경계가 안 보임)
--surface   #0B0F1C   카드
--gold      #D6B24C   유일한 액센트 (화면의 5% 미만에만)
--orb       #7FD0FF   수정구 발광 — 구슬 관련 연출에만
--text      #F0E9DF   캐릭터 털색과 맞춤
--muted     #8B8FA3
```

**폰트** — UI는 Pretendard 계열 산세리프, 명언·핵심문구는 명조체.
딱 2종. 굵기로만 위계를 만든다.

**모션** — 400~700ms, `cubic-bezier(.22,1,.36,1)`. 느릴수록 고급스럽다.

**금지** — 2색 이상 그라데이션 / 이모지 / 파티클 / 폰트 3종 이상 / 200ms 미만 전환 / 입체 버튼

## 폴더 구조

```
assets/
  video/source/     원본 영상 (건드리지 않음)
  video/main/       웹용 압축본 + poster
  character/poses/  부적용 캐릭터 크롭
  brand/logo.png    로고 (투명 PNG)

web/
  index.html            앱 본체 (단일 파일)
  preview-online.html   자동 생성. 직접 수정 금지

content/
  daily/YYYY/MM/*.json  에이전트가 매일 생성하는 하루치
  reference/gold-sample.json  품질 기준선
  pools/                소재·명언·작품 이력

.claude/
  skills/daily-content/ 생성 파이프라인 + 규칙 문서
  agents/               운세·명언·넷플릭스·검수 에이전트
  commands/make-today.md

scripts/
  validate-content.mjs  자동 검증
```

## 콘텐츠 구조

**하루치 = 12띠 × 3연령대 = 36세트**

```
student  ~19세    학교·시험·급식·용돈
worker   20~64세  회의·출근·월급·회식
senior   65세~    건강·모임·정리·산책
```

같은 띠라도 **연령대가 다르면 본문이 완전히 달라야 한다.**
2015년생과 1972년생이 같은 문장을 보면 안 된다.

형식은 `.claude/skills/daily-content/references/format-spec.md` 참조.

## 작업할 때 반드시 지킬 것

### 콘텐츠를 쓸 때
1. `references/tone-guide.md` — 톤과 문장 규칙
2. `references/cliche-blacklist.md` — 금칙어
3. `references/safety-guide.md` — 의료·투자·비하·불안조성 금지

### 하루치를 만들 때
```
/make-today 2026-08-13
/make-today 2026-08-13 7    ← 7일치
```
생성 후 `node scripts/validate-content.mjs <경로>` 통과 필수.

### 앱을 수정한 뒤
`web/preview-online.html` 은 빌드 산출물이다. `web/index.html` 만 고치고,
폰에서 볼 링크가 필요하면 미디어를 인라인한 버전을 다시 빌드한다.

## 알려진 제약

- **영상은 무음 재생만 가능.** 브라우저가 소리 있는 자동재생을 막는다. 볼륨 버튼은 뺀 상태.
- **부적 저장이 아직 가짜.** 버튼만 있고 실제 이미지 저장이 안 된다.
- **수집 기록이 브라우저에만 남는다.** 개발자도구로 조작 가능하므로,
  커피 보상을 실제로 줄 거면 로그인이 필수다.
- **넷플릭스 링크는 `/title/{id}` 만 쓴다.** `/search?q=` 는 로그인을 요구한다.
- 넷플릭스는 작품을 수시로 내리므로 월 1회 링크 점검이 필요하다.

## 로컬에서 보기

```
바탕화면 "위캔두잇 오늘의 운세" 바로가기 더블클릭
또는
python -m http.server 8080  →  http://localhost:8080/web/index.html
폰(같은 와이파이)            →  http://192.168.0.105:8080/web/index.html
```
